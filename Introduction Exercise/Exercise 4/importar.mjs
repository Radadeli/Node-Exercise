// hay que importar la función multiplicar 
import multiplicarFuncion from './exportar.mjs';

// mostrar el resultado en la consola 
const resultado = multiplicarFuncion(3, 7);
console.log(`Resultado de la multiplicación: ${resultado}`);
